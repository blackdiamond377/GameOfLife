<Window x:Class="GameDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameDisplay"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Canvas Name="MainCanvas"/>
        </Grid>
        <Grid Grid.Row="1">
            <Button Name="ResetButton" Content="Reset" HorizontalAlignment="Left"/>
            <Button Name="NextButton" Content="Next" HorizontalAlignment="Left" Width="35" Margin="35,0,0,0"/>
            <Button Name="PlayButton" Content="Play" HorizontalAlignment="Left" Width="35" Margin="70,0,0,0"/>
        </Grid>
    </Grid>
</Window>
